"""
Ingestion Package.

This package manages the entire data ingestion pipeline:
- Loaders: Extracts text from various document formats (PDF, DOCX, PPTX, HTML, TXT).
- Chunking: Breaks large documents into semantic chunks.
- Processor: Orchestrates the parsing and chunking workflow before embedding generation.
"""
